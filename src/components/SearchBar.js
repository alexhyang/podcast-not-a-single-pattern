import React from 'react';

function SearchBar(props) {
    return (
        <div>
            <form>
                <input value="search" />
            </form>
        </div>
    );
}

export default SearchBar;